# TODOs
## Dependencies
Add the following dependencies to be auto-pulled after building the container
- warp-lang  // Optional
- stable-baselines3[extra]  // Only if current training script works

## Performance
- Look into the "low GPU utilization" warning
